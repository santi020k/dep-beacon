---
import { Callout, Link } from '@santi020k/lumen-astro'

import Docs from '../../layouts/Docs.astro'
import { SITE } from '../../site.config'
---

<Docs title="Install" description="Install Dep Beacon for VS Code or Zed.">
  <h1>Install Dep Beacon</h1>
  <p>
    Add Dep Beacon to VS Code or Zed, then open a supported npm manifest.
  </p>

  <Callout class="docs-callout">
    <strong>Install links:</strong>
    <span>
      <Link href={SITE.marketplaceUrl} target="_blank" rel="noopener noreferrer">Visual Studio Marketplace</Link> or
      <Link href={SITE.openVsxUrl} target="_blank" rel="noopener noreferrer">Open VSX</Link>.
    </span>
  </Callout>

  <h2>From VS Code</h2>
  <ol>
    <li>Open the Extensions view.</li>
    <li>Search for `Dep Beacon`.</li>
    <li>Select Install.</li>
    <li>Open `package.json`, `pnpm-workspace.yaml`, or `pnpm-workspace.yml`.</li>
  </ol>

  <h2>From Zed</h2>
  <ol>
    <li>Open the command palette.</li>
    <li>Run `zed: extensions`.</li>
    <li>Search for `Dep Beacon` and select Install.</li>
    <li>Open `package.json`, `pnpm-workspace.yaml`, or `pnpm-workspace.yml`.</li>
  </ol>
  <p>
    See the <Link href="/docs/zed-extension">Zed extension guide</Link> for supported actions, settings, and editor-specific limitations.
  </p>

  <h2>What happens next</h2>
  <p>
    Dep Beacon activates automatically for JSON, JSONC, and YAML dependency files. Both editors show diagnostics and CodeLens actions for supported dependency entries; VS Code also provides inline status labels.
  </p>

  <h2>Useful first VS Code settings</h2>
  <ul>
    <li>Use `depBeacon.showInlineStatus` to show or hide the inline labels.</li>
    <li>Use `depBeacon.checkVulnerabilities` to enable or disable OSV vulnerability checks.</li>
    <li>Use `depBeacon.packageManager` if automatic package-manager detection picks the wrong command.</li>
  </ul>
</Docs>
